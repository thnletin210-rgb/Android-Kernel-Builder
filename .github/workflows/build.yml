name: Build Kernel Viva A16 - Final Success
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Environment
        run: |
          sudo apt-get update
          sudo apt-get install -y bc build-essential zip curl libssl-dev flex bison libncurses-dev python3 git lz4

      - name: Download Toolchain
        run: |
          mkdir -p clang
          curl -L https://github.com/kdrag0n/proton-clang/archive/refs/heads/master.tar.gz -o clang.tar.gz
          tar -xf clang.tar.gz -C clang --strip-components=1
          echo "CLANG_DIR=$(pwd)/clang/bin" >> $GITHUB_ENV

      - name: Clone Source Kernel
        run: |
          git clone --depth=1 https://github.com/xiaomi-mt6781-devs/kernel_xiaomi_viva kernel-source

      - name: Patch KSU Next
        run: |
          cd kernel-source
          curl -LSs "https://raw.githubusercontent.com/rifsytn/KernelSU-Next/main/kernel/setup.sh" | bash -s next

      - name: Patch A16 Config
        run: |
          cd kernel-source
          CONF="arch/arm64/configs/viva_defconfig"
          [ ! -f $CONF ] && CONF=$(find arch/arm64/configs/ -name "*viva*" | head -n 1)
          sed -i 's/# CONFIG_BPF is not set/CONFIG_BPF=y/' $CONF
          echo "CONFIG_BPF=y" >> $CONF
          echo "CONFIG_BPF_SYSCALL=y" >> $CONF
          echo "CONFIG_BPF_JIT=y" >> $CONF
          echo "CONFIG_HAVE_EBPF_JIT=y" >> $CONF
          echo "CONFIG_NET_CLS_BPF=y" >> $CONF
          echo "CONFIG_NET_ACT_BPF=y" >> $CONF
          echo "CONFIG_BPF_EVENTS=y" >> $CONF
          echo "CONFIG_ANDROID_BINDERFS=y" >> $CONF
          echo "CONFIG_SECURITY_SELINUX_DISABLE=y" >> $CONF

      - name: Build Kernel
        run: |
          cd kernel-source
          export PATH="${{ env.CLANG_DIR }}:$PATH"
          export ARCH=arm64
          export SUBARCH=arm64
          export CROSS_COMPILE=aarch64-linux-gnu-
          export CROSS_COMPILE_ARM32=arm-linux-gnueabi-
          export CC=clang
          
          CONF_NAME="viva_defconfig"
          [ ! -f arch/arm64/configs/$CONF_NAME ] && CONF_NAME=$(basename $(find arch/arm64/configs/ -name "*viva*" | head -n 1))

          make O=out $CONF_NAME
          make -j$(nproc --all) O=out \
            NM=llvm-nm \
            OBJCOPY=llvm-objcopy \
            LD=ld.lld \
            STRIP=llvm-strip \
            Image.gz-dtb

      - name: Upload Thành Phẩm
        uses: actions/upload-artifact@v4
        with:
          name: Kernel_Viva_A16
          path: kernel-source/out/arch/arm64/boot/Image.gz-dtb
